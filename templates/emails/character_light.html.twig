{% extends 'base.html.twig' %}

{% block body %}
<main class="container">
  <h1>{{data.name}}</h1>
  <img src="{{data.image}}" alt="{{data.name}}" />
  <h3>{{data.species}}</h3>
</main>
{% endblock %}